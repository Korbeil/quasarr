{% set active = active|default('search') %}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('dashboard') }}">Quasarr</a>
    <button class="navbar-toggler"
            type="button"
            data-toggle="collapse"
            data-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item {{ active == 'search' ? 'active' }}">
                <a class="nav-link" href="{{ path('dashboard') }}">
                    Search
                </a>
            </li>
            <li class="nav-item {{ active == 'movies' ? 'active' }}">
                <a class="nav-link" href="{{ path('list_movies') }}">
                    Movies
                </a>
            </li>
            <li class="nav-item {{ active == 'tvshows' ? 'active' }}">
                <a class="nav-link" href="#">
                    TV Shows
                </a>
            </li>
            <li class="nav-item {{ active == 'settings' ? 'active' }}">
                <a class="nav-link" href="#">
                    Settings
                </a>
            </li>
            <li class="nav-item ml-5">
                <a class="btn btn-outline-primary" href="#">Language</a>
            </li>
        </ul>
    </div>
</nav>
